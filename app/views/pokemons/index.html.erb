<body>
  <div class="pokedex-title">
    <p id="title">The Original Pokedex</p>
  </div>
  <div id="sprites">
    <% @pokemons.each do |pokemon| %>
      <%= link_to image_tag("http://img.pokemondb.net/sprites/black-white/normal/" +  pokemon.name.tr('[]', '').gsub(/[""]/, '') + ".png"), pokemon_path(pokemon) %>
    <% end %>
  </div>
</body>
